{
  "name": "promisebatching",
  "version": "1.0.0",
  "description": "Create a server using Express that has an endpoint: /getPromise/:id. When a request is made, a new Promise should be created, which returns the specified id after 10 seconds. If the server is queried for the same id within the next 10 seconds, it should not create a new Promise but return the result of the previously processed one.",
  "main": "index.js",
  "scripts": {
    "test": "jest"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/dzgierski19/PromiseBatching.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/dzgierski19/PromiseBatching/issues"
  },
  "homepage": "https://github.com/dzgierski19/PromiseBatching#readme",
  "devDependencies": {
    "@types/jest": "^29.5.6",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "ts-node": "^10.9.1"
  },
  "dependencies": {
    "@jest/fake-timers": "^29.7.0",
    "express": "^4.18.2",
    "nodemon": "^3.0.1"
  }
}
